document.addEventListener('DOMContentLoaded',()=>{const $=s=>document.querySelector(s),$$=s=>document.querySelectorAll(s);let loader=$('#loader');if(!loader){loader=document.createElement('div');loader.id='loader';document.body.prepend(loader)}const logo=$('.logo');const brand=(logo?.textContent||document.title.split('—')[0]||'QUEENS BEAUTY PALACE').replace(/\s+/g,' ').trim();loader.innerHTML=`<div class="loader-mark"><strong>${brand}</strong><span>WELCOME</span></div>`;document.body.classList.add('is-loading');requestAnimationFrame(()=>setTimeout(()=>{loader.classList.add('is-hidden');document.body.classList.remove('is-loading');setTimeout(()=>loader.remove(),900)},700));const progress=document.createElement('div');progress.id='scroll-progress';document.body.append(progress);const onScroll=()=>{const h=document.documentElement.scrollHeight-innerHeight;progress.style.width=(h>0?scrollY/h*100:0)+'%';$('header')?.classList.toggle('scrolled',scrollY>30)};addEventListener('scroll',onScroll,{passive:true});onScroll();const io=new IntersectionObserver(entries=>entries.forEach(entry=>{const el=entry.target;if(entry.isIntersecting){el.classList.add('visible');el.classList.remove('exit')}else{el.classList.remove('visible');if(entry.boundingClientRect.top<0)el.classList.add('exit');else el.classList.remove('exit')}}),{threshold:.12,rootMargin:'-8% 0px -8% 0px'});$$('.reveal').forEach((el,i)=>{el.style.transitionDelay=(i%6)*55+'ms';io.observe(el)});$$('.tilt').forEach(card=>{card.addEventListener('pointermove',e=>{if(matchMedia('(hover:none)').matches)return;const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(1000px) rotateX(${y*-4}deg) rotateY(${x*4}deg) translateY(-8px)`});card.addEventListener('pointerleave',()=>card.style.transform='')});$$('.magnetic').forEach(btn=>{btn.addEventListener('pointermove',e=>{if(matchMedia('(hover:none)').matches)return;const r=btn.getBoundingClientRect();btn.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.08}px,${(e.clientY-r.top-r.height/2)*.08}px)`});btn.addEventListener('pointerleave',()=>btn.style.transform='')});$$('a[href*="wa.me"]').forEach(a=>a.setAttribute('aria-label','Book via WhatsApp'));$$('a[href^="tel:"]').forEach(a=>a.setAttribute('aria-label','Call Queens Beauty Palace'))});