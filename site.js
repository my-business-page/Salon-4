document.addEventListener('DOMContentLoaded',()=>{const $=s=>document.querySelector(s),$$=s=>document.querySelectorAll(s);const reduce=matchMedia('(prefers-reduced-motion:reduce)').matches;let loader=$('#loader');if(loader){setTimeout(()=>loader.classList.add('is-hidden'),650);setTimeout(()=>loader.remove(),1500)}const progress=document.createElement('div');progress.id='scroll-progress';document.body.append(progress);const header=$('header');const onScroll=()=>{const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=(max>0?scrollY/max*100:0)+'%';header?.classList.toggle('scrolled',scrollY>24)};addEventListener('scroll',onScroll,{passive:true});onScroll();const reveal=$$('.reveal');if(reduce){reveal.forEach(e=>e.classList.add('visible'))}else{const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');e.target.classList.remove('exit')}else{e.target.classList.remove('visible');if(e.boundingClientRect.top<0)e.target.classList.add('exit')}}),{threshold:.1,rootMargin:'-7% 0px -7% 0px'});reveal.forEach((el,i)=>{el.style.transitionDelay=(i%5)*60+'ms';io.observe(el)})}if(!reduce&&!matchMedia('(hover:none)').matches){$$('.tilt').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect();const x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateX(${y*-3}deg) rotateY(${x*3}deg) translateY(-7px)`});card.addEventListener('pointerleave',()=>card.style.transform='')});$$('.magnetic').forEach(btn=>{btn.addEventListener('pointermove',e=>{const r=btn.getBoundingClientRect();btn.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.06}px,${(e.clientY-r.top-r.height/2)*.06}px)`});btn.addEventListener('pointerleave',()=>btn.style.transform='')})}$$('a[href*="wa.me"]').forEach(a=>a.setAttribute('aria-label','Book Queens Beauty Palace on WhatsApp'));$$('a[href^="tel:"]').forEach(a=>a.setAttribute('aria-label','Call Queens Beauty Palace'))});